name: Status check functions
on: push
jobs:
  fail:
    runs-on: ubuntu-latest
    steps:
      - run: exit 0
      - run: echo "failure"
        if: ${{ failure() }}
      - run: echo "success"
        if: ${{ success() }}
